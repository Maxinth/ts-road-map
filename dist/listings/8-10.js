"use strict";
/* Using Functions */
/*

// using a rest parameters
function calculateTax(
  amount: number,
  discount: number = 0,
  ...extraFees: number[]
): number {
  return (
    amount * 1.2 - discount + extraFees.reduce((total, val) => total + val)
  );
}
let taxValue = calculateTax(100, 10, 20, 1, 30, 7);
console.log(`6 args: ${taxValue}`);

*/
//# sourceMappingURL=8-10.js.map