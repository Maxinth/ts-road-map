/**
 * Mastering typeScript
 */

let index: number = 0;

if (index == 0) {
  let index: number = 2;
  console.log(`index inside if block = ${index}`);
}

console.log(`index  outside if block = ${index}`);
